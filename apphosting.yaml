# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase instances for production availability
  minInstances: 0
  maxInstances: 10
  # Ensure enough memory for Genkit/AI processing
  memory: 1Gi

env:
  - variable: NODE_ENV
    value: production
